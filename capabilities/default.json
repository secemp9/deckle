{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Deckle main window (embedded assets)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
